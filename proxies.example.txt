# One SOCKS5 upstream per line. Blank lines and #-comments are ignored.
# Accepted formats:
#   socks5://host:port
#   socks5://user:pass@host:port
#   host:port:user:pass
#   user:pass@host:port
#
# Copy to proxies.txt (gitignored: it carries credentials), then reload with
# SIGHUP. Runtime state is preserved only for unchanged route URLs.
proxy1.example.com:1080:username:password
user:pass@proxy2.example.com:1080
